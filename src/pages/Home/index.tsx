import React from 'react'
import styles from './index.module.less'

type Props = {}

const index = (props: Props) => {
    return (
        <div className={styles.ase}>index</div>
    )
}

export default index